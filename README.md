# PDF Reader Application (Python)

A backend-based PDF Reader application built using Python that processes PDF files, integrates AWS services, and stores metadata using MySQL.

## 🛠️ Technologies Used
- Python
- AWS (S3 / EC2)
- MySQL (MySQL Workbench)

## 📂 Project Files
- `final.py` – Main application logic
- `test.py` – Testing and helper functions
- `db.py` – Database connection and queries

## ⚙️ How It Works
1. PDF files are uploaded and processed using Python
2. Files are stored or accessed via AWS services
3. Metadata is stored in a MySQL database
4. Database operations are handled through MySQL Workbench

## 🚀 How to Run Locally
1. Clone the repository  
   ```bash
   git clone https://github.com/your-username/pdf-reader-backend.git
